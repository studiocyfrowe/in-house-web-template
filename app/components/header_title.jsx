export default function HeaderTitle({label}) {
    return (
        <h1 className="text-3xl lg:text-6xl uppercase tracking-[.25em] hover:tracking-[.05em] mt-24 lg:mt-48 transition-all cursor-pointer">{label}</h1>
    )
}